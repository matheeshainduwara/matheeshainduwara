name: 🔄 Auto-Update Profile README

on:
  schedule:
    - cron: "0 * * * *"
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  update-readme:
    name: 📝 Update README
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git pull origin main

      - name: Update greeting based on time
        run: |
          python3 << 'EOF'
          from datetime import datetime
          import re
          from zoneinfo import ZoneInfo

          hour = datetime.now(ZoneInfo("Asia/Colombo")).hour

          print(f"Current Sri Lanka hour: {hour}")

          if 5 <= hour < 12:
              greeting = "Good morning! - ready to build something cool today"
              cache_token = "morning"
          elif 12 <= hour < 16:
              greeting = "Good afternoon! - hope the code is flowing well"
              cache_token = "afternoon"
          elif 16 <= hour < 21:
              greeting = "Good evening! - thanks for checking out my work"
              cache_token = "evening"
          else:
              greeting = "Burning the midnight oil too - welcome!"
              cache_token = "night"

          print(f"Theme: {cache_token}")
          print(f"Greeting: {greeting}")

          # Only replace spaces with + no other encoding
          encoded_greeting = greeting.replace(" ", "+")

          with open("README.md", "r", encoding="utf-8") as f:
              readme = f.read()

          new_greeting = f'<div align="center">\n<img src="https://readme-typing-svg.demolab.com?font=Orbitron&weight=500&size=14&duration=0&pause=99999&color=38BDF8&center=true&vCenter=true&width=600&height=45&lines={encoded_greeting}&v={cache_token}" alt="greeting"/>\n</div>'

          greeting_pattern = r"<!-- GREETING:START -->.*?<!-- GREETING:END -->"
          greeting_replacement = f"<!-- GREETING:START -->\n{new_greeting}\n<!-- GREETING:END -->"
          new_readme = re.sub(greeting_pattern, greeting_replacement, readme, flags=re.DOTALL)

          with open("README.md", "w", encoding="utf-8") as f:
              f.write(new_readme)

          print("Greeting updated!")
          EOF

      - name: Fetch Medium posts and update README
        run: |
          python3 << 'EOF'
          import urllib.request
          import xml.etree.ElementTree as ET
          import re
          from datetime import datetime

          url = "https://medium.com/feed/@matheeshainduwara"
          req = urllib.request.Request(url, headers={"User-Agent": "Mozilla/5.0"})
          with urllib.request.urlopen(req) as response:
              xml_data = response.read()

          root = ET.fromstring(xml_data)
          channel = root.find("channel")
          items = channel.findall("item")[:3]

          lines = []
          for item in items:
              title = item.find("title").text.strip().rstrip(".")
              raw_url = item.find("link").text.strip()
              clean_url = raw_url.split("?source=")[0]
              pub_date = item.find("pubDate").text.strip()
              try:
                  dt = datetime.strptime(pub_date, "%a, %d %b %Y %H:%M:%S %Z")
                  date_str = dt.strftime("%b %d, %Y")
              except:
                  date_str = pub_date[:16]
              lines.append(f"- 📝 **[{title}]({clean_url})** — `{date_str}`")

          blog_content = "\n".join(lines)

          with open("README.md", "r", encoding="utf-8") as f:
              readme = f.read()

          pattern = r"<!-- BLOG-POST-LIST:START -->.*?<!-- BLOG-POST-LIST:END -->"
          replacement = f"<!-- BLOG-POST-LIST:START -->\n{blog_content}\n<!-- BLOG-POST-LIST:END -->"
          new_readme = re.sub(pattern, replacement, readme, flags=re.DOTALL)

          with open("README.md", "w", encoding="utf-8") as f:
              f.write(new_readme)

          print("Blog posts updated!")
          EOF

      - name: Update timestamp badge
        run: |
          DATE=$(date +'%B %d, %Y' | sed 's/ /%20/g')
          sed -i "s|Last%20Updated-[^-]*-|Last%20Updated-${DATE}-|g" README.md
          echo "Timestamp updated!"

      - name: Commit and push changes
        run: |
          git add README.md
          git diff --staged --quiet || git commit -m "chore(readme): update greeting, blog posts & timestamp [skip ci]"
          git push origin main
